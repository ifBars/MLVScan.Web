import { Link } from "react-router-dom"

export default function RuntimeOverviewContent() {
  return (
    <>
      <p className="text-gray-300 mb-6">
        MLVScan runtime packages run before mod execution and enforce policy from the Core
        threat-disposition pipeline. The plugin treats known threat-family matches as likely malware,
        surfaces correlated suspicious behavior separately, and keeps incomplete scans in a manual-review
        state instead of collapsing them into suspicious by default.
      </p>

      <h2 id="supported-loaders">Supported Loaders</h2>
      <ul className="list-disc list-inside space-y-2 text-gray-300 mb-8">
        <li>MelonLoader (v0.6+)</li>
        <li>BepInEx 5.x (Patcher flow)</li>
        <li>BepInEx 6.x Mono (Plugin flow)</li>
        <li>BepInEx 6.x IL2CPP (Plugin flow)</li>
      </ul>

      <h2 id="what-it-controls">What It Controls</h2>
      <ul className="list-disc list-inside space-y-2 text-gray-300 mb-8">
        <li>Automatic scanning with <code>EnableAutoScan</code>.</li>
        <li>Automatic blocking with <code>EnableAutoDisable</code>.</li>
        <li>Authenticated local result reuse with <code>EnableScanCache</code>.</li>
        <li>Verdict-based blocking with <code>BlockKnownThreats</code>, <code>BlockSuspicious</code>, and <code>BlockIncompleteScans</code>.</li>
        <li>Hash-based trust decisions with <code>WhitelistedHashes</code>.</li>
        <li>Optional full IL output in MelonLoader with <code>DumpFullIlReports</code>.</li>
        <li>Optional report upload with <code>EnableReportUpload</code>.</li>
      </ul>

      <h2 id="where-to-go-next">Where To Go Next</h2>
      <div className="space-y-2 text-teal-400">
        <Link to="/docs/libraries/mlvscan/getting-started" className="block hover:text-teal-300">Getting Started</Link>
        <Link to="/docs/libraries/mlvscan/architecture" className="block hover:text-teal-300">Architecture</Link>
        <Link to="/docs/libraries/mlvscan/whitelisting" className="block hover:text-teal-300">Whitelisting</Link>
        <Link to="/docs/libraries/mlvscan/reports-and-triage" className="block hover:text-teal-300">Reports and Triage</Link>
        <Link to="/docs/libraries/mlvscan/faq-and-troubleshooting" className="block hover:text-teal-300">FAQ and Troubleshooting</Link>
        <Link to="/docs/libraries/devcli" className="block hover:text-teal-300">Developer CLI</Link>
      </div>

      <p className="text-gray-400 text-sm mt-6">
        The runtime docs focus on loader behavior. If you are building a mod project or CI check, use the
        DevCLI docs instead of treating the runtime plugin as the authoring path.
      </p>
    </>
  )
}
