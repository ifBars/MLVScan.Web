import { Link } from "react-router-dom"
import { CodeBlock } from "@/components/docs/mdx/CodeBlock"

export default function RuntimeWhitelistingContent() {
  return (
    <>
      <p className="text-gray-300 mb-6">
        MLVScan uses SHA256 hashes for trust decisions so the whitelist follows the exact binary, not the
        filename or install location. This keeps the runtime aligned with the verdict-first pipeline and
        avoids coupling trust to a specific loader threshold.
      </p>

      <h2 id="why-hash-based">Why Hash-Based</h2>
      <ul className="list-disc list-inside space-y-2 text-gray-300 mb-6">
        <li>SHA256 detects tampering across file renames.</li>
        <li>Trust is pinned to exact binary content, not filename.</li>
        <li>Hash-based allow lists work across MelonLoader and BepInEx installations.</li>
      </ul>

      <h2 id="workflow">Recommended Workflow</h2>
      <ol className="list-decimal list-inside space-y-2 text-gray-300 mb-6">
        <li>Collect the hash from report output.</li>
        <li>Verify source and ask community for confirmation.</li>
        <li>Add hash to <code>WhitelistedHashes</code> only after verification.</li>
      </ol>

      <p className="text-gray-300 mb-6">
        MLVScan also merges a small built-in whitelist for known-safe false positives. Keep your own
        additions narrow and exact instead of copying broad shared allow lists between communities.
      </p>

      <h2 id="config-example">Config Example</h2>
      <p className="text-gray-300 mb-4">
        The whitelist keys are the same for MelonLoader and BepInEx. Use the loader-specific config
        file location, then add the verified hash to <code>WhitelistedHashes</code>.
      </p>
      <CodeBlock language="ini" className="mb-6">{`[MLVScan]
EnableAutoScan = true
EnableAutoDisable = true
EnableScanCache = true
BlockKnownThreats = true
BlockSuspicious = true
BlockIncompleteScans = false
WhitelistedHashes = ["<verified SHA256 hash>"]
DumpFullIlReports = false
EnableReportUpload = false`}</CodeBlock>

      <h2 id="manual-hash">Manual Hash Check</h2>
      <CodeBlock language="powershell" className="mb-6">{`Get-FileHash -Algorithm SHA256 "path\\to\\mod.dll"`}</CodeBlock>

      <p className="text-gray-300">
        If you are building a mod or CI workflow, use the{" "}
        <Link to="/docs/libraries/devcli" className="text-teal-400 hover:text-teal-300">
          Developer CLI
        </Link>{" "}
        docs for scan-time automation instead of copying runtime settings into your build pipeline.
      </p>
    </>
  )
}
