export default function CoreGettingStartedContent() {
  return (
    <>
      <p className="text-gray-300 mb-6">
        This page is a short bridge into the richer DocFX guides. Start with{' '}
        <a href="/docs/reference/core/guides/using-mlvscan-core.html" className="text-teal-400 hover:text-teal-300">
          Using MLVScan.Core
        </a>
        , then move to the dedicated guides for the detection pipeline, call-chain analysis, and data-flow analysis.
      </p>

      <h2 id="quick-start">Quick Start</h2>
      <pre className="bg-gray-800 rounded-lg p-4 mb-6 overflow-x-auto"><code>{`dotnet add package MLVScan.Core`}</code></pre>

      <p className="text-gray-300 mb-4">
        Create the default rule set, scan an assembly, and map the result when you need the shared schema contract.
      </p>

      <pre className="bg-gray-800 rounded-lg p-4 mb-6 overflow-x-auto"><code>{`using MLVScan;
using MLVScan.Models.Dto;
using MLVScan.Services;

var assemblyPath = "MyMod.dll";
var assemblyBytes = File.ReadAllBytes(assemblyPath);
var rules = RuleFactory.CreateDefaultRules();
var scanner = new AssemblyScanner(rules);
var findings = scanner.Scan(assemblyPath).ToList();

var result = ScanResultMapper.ToDto(
    findings,
    Path.GetFileName(assemblyPath),
    assemblyBytes,
    new ScanResultOptions
    {
        Platform = "my-tool",
        PlatformVersion = "1.0.0"
    });

Console.WriteLine(result.Disposition?.Classification);`}</code></pre>

      <h2 id="next-steps">Next Steps</h2>
      <ul className="list-disc list-inside space-y-2 text-gray-300">
        <li>
          <a href="/docs/reference/core/guides/detection-pipeline.html" className="text-teal-400 hover:text-teal-300">
            Detection Pipeline
          </a>
        </li>
        <li>
          <a href="/docs/reference/core/guides/call-chain-analysis.html" className="text-teal-400 hover:text-teal-300">
            Call Chain Analysis
          </a>
        </li>
        <li>
          <a href="/docs/reference/core/guides/data-flow-analysis.html" className="text-teal-400 hover:text-teal-300">
            Data Flow Analysis
          </a>
        </li>
        <li>
          <a href="/docs/reference/core/guides/extending-mlvscan-core.html" className="text-teal-400 hover:text-teal-300">
            Extending MLVScan.Core
          </a>
        </li>
      </ul>
    </>
  )
}
