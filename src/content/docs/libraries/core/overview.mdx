import { Link } from "react-router-dom"

export default function CoreOverviewContent() {
  return (
    <>
      <p className="text-gray-300 mb-6">
        MLVScan.Core is the shared detection engine used by runtime mods, the web scanner, and DevCLI.
        It provides static IL analysis via Mono.Cecil, threat-family classification, and structured scan output.
      </p>

      <div className="rounded-lg border border-teal-900/50 bg-teal-950/20 p-4 mb-8">
        <h3 className="text-teal-300 font-semibold mb-2">Shared Scan Contract</h3>
        <p className="text-sm text-gray-300 mb-2">
          The public scan-result contract includes scan metadata, retained findings, threat-family matches,
          and the primary file disposition. JSON Schema and shared TypeScript definitions are generated from
          the Core DTOs so every consumer reads the same payload shape.
        </p>
        <p className="text-sm text-gray-300">
          See <Link to="/docs/libraries/core/scan-result-schema" className="text-teal-300 hover:text-teal-200">Scan Result Schema</Link> for the published fields and consumption guidance.
        </p>
      </div>

      <h2 id="what-you-get">What You Get</h2>
      <ul className="list-disc list-inside space-y-2 text-gray-300 mb-8">
        <li>Threat-family matching for known malware samples and recurring malware behavior patterns.</li>
        <li>Primary file disposition values: <code>Clean</code>, <code>Suspicious</code>, and <code>KnownThreat</code>.</li>
        <li>Default findings for the retained verdict, plus advanced diagnostics for lower-confidence supporting signals.</li>
        <li>Call-chain consolidation for traceable entry-point paths.</li>
        <li>Data-flow tracking for multi-step attack patterns.</li>
        <li>A shared result format used across the web scanner, WASM package, API, and desktop tools.</li>
      </ul>

      <h2 id="next-pages">Next Pages</h2>
      <div className="space-y-2 text-teal-400">
        <Link to="/docs/libraries/core/getting-started" className="block hover:text-teal-300">Getting Started</Link>
        <Link to="/docs/libraries/core/advanced-analysis" className="block hover:text-teal-300">Advanced Analysis</Link>
        <Link to="/docs/libraries/core/reference" className="block hover:text-teal-300">Core Reference</Link>
        <Link to="/docs/libraries/core/scan-result-schema" className="block hover:text-teal-300">Scan Result Schema</Link>
      </div>
    </>
  )
}
