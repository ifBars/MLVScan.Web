import { Link } from "react-router-dom"

export default function CoreOverviewContent() {
  return (
    <>
      <p className="text-gray-300 mb-6">
        MLVScan.Core is the shared detection engine used by runtime mods, the web scanner, and DevCLI.
        It provides static IL analysis via Mono.Cecil, rule execution, and structured output.
      </p>

      <div className="rounded-lg border border-teal-900/50 bg-teal-950/20 p-4 mb-8">
        <h3 className="text-teal-300 font-semibold mb-2">Single Source of Truth</h3>
        <p className="text-sm text-gray-300 mb-2">
          The public scan-result contract lives in <code>MLVScan.Core/Models/Dto/</code>. JSON Schema and shared TypeScript definitions are generated from those DTOs so consumers do not hand-maintain copies.
        </p>
        <p className="text-sm text-gray-300">
          See <Link to="/docs/libraries/core/scan-result-schema" className="text-teal-300 hover:text-teal-200">Scan Result Schema</Link> for the generation workflow and artifact locations.
        </p>
      </div>

      <h2 id="what-you-get">What You Get</h2>
      <ul className="list-disc list-inside space-y-2 text-gray-300 mb-8">
        <li>Rule-based detections grouped by severity.</li>
        <li>Call-chain consolidation for traceable entry-point paths.</li>
        <li>Data-flow tracking for multi-step attack patterns.</li>
        <li>Core-owned schema artifacts reused across WASM, Web, API, and desktop tools.</li>
      </ul>

      <h2 id="next-pages">Next Pages</h2>
      <div className="space-y-2 text-teal-400">
        <Link to="/docs/libraries/core/getting-started" className="block hover:text-teal-300">Getting Started</Link>
        <Link to="/docs/libraries/core/advanced-analysis" className="block hover:text-teal-300">Advanced Analysis</Link>
        <Link to="/docs/libraries/core/api-reference" className="block hover:text-teal-300">API Reference</Link>
        <Link to="/docs/libraries/core/scan-result-schema" className="block hover:text-teal-300">Scan Result Schema</Link>
      </div>
    </>
  )
}
