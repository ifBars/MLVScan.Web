export default function DevCliUsageAndCiContent() {
  return (
    <>
      <h2 id="install">Install</h2>
      <pre className="bg-gray-800 rounded-lg p-4 mb-6 overflow-x-auto"><code>{`dotnet tool install --global MLVScan.DevCLI`}</code></pre>

      <h2 id="common-commands">Common Commands</h2>
      <pre className="bg-gray-800 rounded-lg p-4 mb-6 overflow-x-auto"><code>{`mlvscan MyMod.dll
mlvscan MyMod.dll --format schema
mlvscan MyMod.dll --fail-on-disposition Suspicious
mlvscan MyMod.dll --fail-on High
mlvscan MyMod.dll --verbose`}</code></pre>

      <p className="text-gray-300 mb-6">
        Prefer <code>--fail-on-disposition</code> for CI because it follows the family-first verdict model.
        <code>--fail-on</code> is still available when you need legacy severity-based gating.
      </p>

      <h2 id="msbuild">MSBuild Hook</h2>
      <pre className="bg-gray-800 rounded-lg p-4 mb-6 overflow-x-auto"><code>{`<Target Name="MLVScanCheck" AfterTargets="Build">
  <Exec Command="dotnet tool run mlvscan -- &quot;$(TargetPath)&quot; --fail-on-disposition Suspicious" />
</Target>`}</code></pre>

      <h2 id="github-actions">GitHub Actions</h2>
      <p className="text-gray-300 mb-4">
        If you want to publish a public MLVScan attestation from CI instead of only running the
        local CLI gate, continue with the{" "}
        <a href="/docs/ci-attestations" className="text-teal-400 hover:text-teal-300">
          CI Attestations guide
        </a>
        .
      </p>
      <pre className="bg-gray-800 rounded-lg p-4 mb-6 overflow-x-auto"><code>{`- name: Install MLVScan.DevCLI
  run: dotnet tool install --global MLVScan.DevCLI

- name: Scan build output
  run: mlvscan ./bin/Release/netstandard2.1/MyMod.dll --format schema > scan-results.json`}</code></pre>
    </>
  )
}
