import { Link } from "react-router-dom"

export default function OverviewContent() {
  const indexCardClass =
    "rounded-lg border border-gray-700 bg-gray-900/40 px-4 py-2.5 hover:border-teal-500 transition-colors"
  const indexCardTitleClass = "mt-0 mb-1.5 text-lg text-white font-semibold leading-snug"
  const indexCardDescriptionClass = "m-0 text-sm text-gray-400 leading-5"

  return (
    <>
      <p className="text-lg text-gray-200 mb-6">
        MLVScan is a comprehensive security ecosystem for Unity modding. This documentation covers the core engine, web scanner, CLI tools, and runtime protection modules.
      </p>

      <div className="rounded-lg border border-amber-900/50 bg-amber-950/20 p-4 mb-8">
        <h3 className="mt-0 text-amber-400 font-semibold mb-2">Why MLVScan?</h3>
        <p className="text-sm text-gray-300 mb-2">
          Unity mods are compiled .NET assemblies loaded directly into the game process. They have full access to the .NET runtime and Unity engine - the same flexibility that enables powerful mods also makes them attractive to attackers.
        </p>
        <p className="text-sm text-gray-300 mb-2">
          Most modders trust platforms like <strong>Thunderstore</strong> or <strong>NexusMods</strong>, which scan files with <strong>VirusTotal</strong>. However, sophisticated threats slip through because attackers don't embed payloads directly in the mod. Instead, they use first-stage loaders - a small amount of code that downloads and executes the real malware at runtime.
        </p>
        <p className="text-sm text-gray-300 mb-2">
          A common pattern: the mod calls <code>Process.Start("powershell.exe", ...)</code> with arguments that download a script to <code>%TEMP%</code>, execute it, then delete it. The payload lives on a remote server and never touches the assembly. VirusTotal doesn't flag this because launching PowerShell isn't inherently suspicious.
        </p>
        <p className="text-sm text-gray-300">
          <strong>MLVScan targets these patterns specifically.</strong> It analyzes bytecode to detect process spawning, high-risk P/Invoke declarations, and encoded strings designed to hide malicious behavior. It's a second line of defense that catches what signature-based scanning misses.
        </p>
      </div>

      <h2 id="library-map">Library Map</h2>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-3 mb-8">
        <Link to="/docs/libraries/core" className={indexCardClass}>
          <h3 className={indexCardTitleClass}>MLVScan.Core</h3>
          <p className={indexCardDescriptionClass}>Core scanning APIs, result formats, threat-family detection, and advanced analysis.</p>
        </Link>
        <Link to="/docs/libraries/wasm" className={indexCardClass}>
          <h3 className={indexCardTitleClass}>MLVScan.WASM</h3>
          <p className={indexCardDescriptionClass}>Browser runtime model and JS interop patterns.</p>
        </Link>
        <Link to="/docs/libraries/devcli" className={indexCardClass}>
          <h3 className={indexCardTitleClass}>MLVScan.DevCLI</h3>
          <p className={indexCardDescriptionClass}>CLI usage, automation-friendly exit behavior, and command patterns.</p>
        </Link>
        <Link to="/docs/libraries/mlvscan" className={indexCardClass}>
          <h3 className={indexCardTitleClass}>MLVScan Runtime</h3>
          <p className={indexCardDescriptionClass}>MelonLoader/BepInEx operations, whitelisting, and triage.</p>
        </Link>
      </div>

      <h2 id="developer-and-platform-adoption">Developer and Platform Adoption</h2>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-3 mb-8">
        <Link to="/docs/api-adoption" className={indexCardClass}>
          <h3 className={indexCardTitleClass}>API Adoption</h3>
          <p className={indexCardDescriptionClass}>Hosted API overview, authentication, limits, and adoption paths.</p>
        </Link>
        <Link to="/docs/api-integration-flow" className={indexCardClass}>
          <h3 className={indexCardTitleClass}>API Integration Flow</h3>
          <p className={indexCardDescriptionClass}>Upload artifacts, poll reports, reuse known hashes, and publish attestations.</p>
        </Link>
        <Link to="/docs/api-sdks" className={indexCardClass}>
          <h3 className={indexCardTitleClass}>API SDKs</h3>
          <p className={indexCardDescriptionClass}>C# and TypeScript SDKs for hosted API integrations.</p>
        </Link>
        <Link to="/docs/ci-attestations" className={indexCardClass}>
          <h3 className={indexCardTitleClass}>CI Attestations</h3>
          <p className={indexCardDescriptionClass}>GitHub Actions workflow for release-time attestation publishing.</p>
        </Link>
      </div>
    </>
  )
}
