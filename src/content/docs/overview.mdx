import { Link } from "react-router-dom"

export default function OverviewContent() {
  return (
    <>
      <p className="text-lg text-gray-200 mb-6">
        MLVScan is a comprehensive security ecosystem for Unity modding. This documentation covers the core engine, web scanner, CLI tools, and runtime protection modules.
      </p>

      <div className="rounded-lg border border-amber-900/50 bg-amber-950/20 p-4 mb-8">
        <h3 className="text-amber-400 font-semibold mb-2">Why MLVScan?</h3>
        <p className="text-sm text-gray-300 mb-2">
          Unity mods are compiled .NET assemblies loaded directly into the game process. They have full access to the .NET runtime and Unity engine - the same flexibility that enables powerful mods also makes them attractive to attackers.
        </p>
        <p className="text-sm text-gray-300 mb-2">
          Most modders trust platforms like <strong>Thunderstore</strong> or <strong>NexusMods</strong>, which scan files with <strong>VirusTotal</strong>. However, sophisticated threats slip through because attackers don't embed payloads directly in the mod. Instead, they use first-stage loaders - a small amount of code that downloads and executes the real malware at runtime.
        </p>
        <p className="text-sm text-gray-300 mb-2">
          A common pattern: the mod calls <code>Process.Start("powershell.exe", ...)</code> with arguments that download a script to <code>%TEMP%</code>, execute it, then delete it. The payload lives on a remote server and never touches the assembly. VirusTotal doesn't flag this because launching PowerShell isn't inherently suspicious.
        </p>
        <p className="text-sm text-gray-300">
          <strong>MLVScan targets these patterns specifically.</strong> It analyzes bytecode to detect process spawning, high-risk P/Invoke declarations, and encoded strings designed to hide malicious behavior. It's a second line of defense that catches what signature-based scanning misses.
        </p>
      </div>

      <div className="rounded-lg border border-teal-900/50 bg-teal-950/20 p-4 mb-8">
        <h3 className="text-teal-300 font-semibold mb-2">Schema Ownership</h3>
        <p className="text-sm text-gray-300 mb-2">
          The shared scan-result contract is now owned centrally in <code>MLVScan.Core/Models/Dto/</code>. JSON Schema and shared TypeScript definitions are generated from Core and reused by the WASM package, web app, API, and desktop tools.
        </p>
        <p className="text-sm text-gray-300">
          If you are touching result payloads, start with <Link to="/docs/libraries/core/scan-result-schema" className="text-teal-300 hover:text-teal-200">Scan Result Schema</Link> before editing consumer docs or UI code.
        </p>
      </div>

      <h2 id="library-map">Library Map</h2>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
        <Link to="/docs/libraries/core" className="rounded-lg border border-gray-700 bg-gray-900/40 p-4 hover:border-teal-500 transition-colors">
          <h3 className="text-lg text-white font-semibold">MLVScan.Core</h3>
          <p className="text-sm text-gray-400">Engine internals, API surface, schema ownership, and advanced analysis.</p>
        </Link>
        <Link to="/docs/libraries/wasm" className="rounded-lg border border-gray-700 bg-gray-900/40 p-4 hover:border-teal-500 transition-colors">
          <h3 className="text-lg text-white font-semibold">MLVScan.WASM</h3>
          <p className="text-sm text-gray-400">Browser runtime model and JS interop patterns.</p>
        </Link>
        <Link to="/docs/libraries/devcli" className="rounded-lg border border-gray-700 bg-gray-900/40 p-4 hover:border-teal-500 transition-colors">
          <h3 className="text-lg text-white font-semibold">MLVScan.DevCLI</h3>
          <p className="text-sm text-gray-400">CLI usage, CI integration, and command architecture.</p>
        </Link>
        <Link to="/docs/libraries/mlvscan" className="rounded-lg border border-gray-700 bg-gray-900/40 p-4 hover:border-teal-500 transition-colors">
          <h3 className="text-lg text-white font-semibold">MLVScan Runtime</h3>
          <p className="text-sm text-gray-400">MelonLoader/BepInEx operations, whitelisting, and triage.</p>
        </Link>
      </div>
    </>
  )
}
