export default function WebClientStageExecFamilyContent() {
  return (
    <>
      <h2>Behavior Pattern</h2>
      <p>
        This family performs a direct network download with <code>System.Net.WebClient</code>, stages the payload under
        <code> %TEMP%</code>, then executes it with hidden or shell-assisted process launch.
      </p>

      <pre className="bg-gray-950 border border-gray-800 rounded-lg p-4 overflow-x-auto mb-6">
        <code>{String.raw`new WebClient()
-> DownloadFileTaskAsync(url, tempPath)
-> if (File.Exists(tempPath))
   -> Process.Start(tempPath)
   or Process.Start("cmd.exe", "/c \"%TEMP%/d.bat\"")
   or Process.Start("powershell.exe", "-ExecutionPolicy Bypass -File \"%TEMP%/da.ps1\"")`}</code>
      </pre>

      <h2>Why It Clusters Cleanly</h2>
      <ul>
        <li>Shared async <code>WebClient.DownloadFileTaskAsync</code> staging behavior</li>
        <li>Payload lands in TEMP before execution</li>
        <li>Process launch carries hidden-window or shell-execute semantics</li>
        <li>Observed variants execute a batch file, direct executable, or staged PowerShell script</li>
      </ul>

      <h2>Observed Variants</h2>
      <p>
        Malicious reuploads observed as <code>MoreTrees.dll.di</code>, <code>MelonLoaderMod55.dll.di</code>, and
        <code> NoPolice.dll.di</code> share this broader download-stage-execute family.
      </p>

      <h2>Key Signals In MLVScan</h2>
      <ul>
        <li><code>DataInfiltrationRule</code> on known malicious delivery infrastructure</li>
        <li><code>ProcessStartRule</code> with TEMP staging and execution</li>
        <li>Correlated download-and-execute data flow</li>
      </ul>
    </>
  )
}
