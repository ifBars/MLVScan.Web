export default function DynamicAssemblyReflectionLoaderFamilyContent() {
  return (
    <>
      <h2>Behavior Pattern</h2>
      <p>
        This family loads opaque assembly bytes at runtime and invokes code through reflection. The
        visible mod can remain small or legitimate-looking while executable behavior is delegated to
        a hidden dynamic assembly or plugin payload.
      </p>

      <pre className="bg-gray-950 border border-gray-800 rounded-lg p-4 overflow-x-auto mb-6">
        <code>{String.raw`File.ReadAllBytes(...) or embedded resource bytes
-> Assembly.Load(byte[])
-> GetType / GetMethod
-> MethodInfo.Invoke(...)`}</code>
      </pre>

      <h2>Why It Clusters Cleanly</h2>
      <ul>
        <li>Runtime assembly loading separates the inspected wrapper from the code that actually runs</li>
        <li>Reflection uses unresolved or non-literal method names to hide the invoked target</li>
        <li>Some variants combine plugin assembly loading with hidden process launch behavior</li>
      </ul>

      <h2>Observed Variants</h2>
      <p>
        Current quarantine coverage clusters behavior observed in <code>FPSCounter.dll</code>,{" "}
        <code>GUITweaks.dll</code>, <code>RemoveBlur.dll</code>, <code>RemovePixelation.dll</code>,{" "}
        <code>YAPYAPFPSCounter.dll</code>, <code>ZeroDeathDrops.dll</code>, <code>iiModdedV5.dll</code>,{" "}
        and <code>XZRAV1.dll</code>.
      </p>

      <h2>Key Signals In MLVScan</h2>
      <ul>
        <li><code>AssemblyDynamicLoadRule</code> on dynamic assembly byte loading</li>
        <li><code>ReflectionRule</code> on unresolved reflected invocation</li>
        <li><code>DataFlowAnalysis</code> when file bytes flow into <code>Assembly.Load</code></li>
      </ul>
    </>
  )
}
