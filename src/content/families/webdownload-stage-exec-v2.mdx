export default function WebDownloadStageExecFamilyContent() {
  return (
    <>
      <h2>Behavior Pattern</h2>
      <p>
        This family performs a direct network download with <code>System.Net.WebClient</code>, <code>System.Net.Http.HttpClient</code>,
        or a similar API, stages the payload under <code>%TEMP%</code>, then executes it with hidden or shell-assisted process launch.
      </p>

      <pre className="bg-gray-950 border border-gray-800 rounded-lg p-4 overflow-x-auto mb-6">
        <code>{String.raw`new WebClient() or new HttpClient()
-> DownloadFileTaskAsync(url, tempPath)
or GetByteArrayAsync(url) -> File.WriteAllBytes(tempPath, bytes)
-> if (File.Exists(tempPath))
   -> Process.Start(tempPath)
   or Process.Start("cmd.exe", "/c \"%TEMP%/d.bat\"")
   or Process.Start("powershell.exe", "-ExecutionPolicy Bypass -File \"%TEMP%/da.ps1\"")`}</code>
      </pre>

      <h2>Why It Clusters Cleanly</h2>
      <ul>
        <li>Shared network-download to temp-staging behavior even when the downloader API changes</li>
        <li>Payload lands in TEMP before execution</li>
        <li>Process launch carries hidden-window or shell-execute semantics</li>
        <li>Observed variants execute a batch file, direct executable, or staged PowerShell script</li>
      </ul>

      <h2>Observed Variants</h2>
      <p>
        Malicious reuploads observed as <code>MoreTrees.dll</code>,{" "}
        <code>MelonLoaderMod55.dll</code>, <code>NoPolice.dll</code>,{" "}
        <code>LongLastingFertilizer.dll</code>, <code>RentalCars.dll</code>,{" "}
        <code>Skitching.dll</code>, <code>StorageHub.dll</code>,{" "}
        <code>vortex_backuprtilizer.dll</code>, and{" "}
        <code>UnlimitedGraffiti.dll</code> share this broader download-stage-execute family. The paired advisory also
        documents a later April 3, 2026 reupload of the same mod lineage under the upload name <code>No Art Limit</code>.
      </p>

      <h2>Key Signals In MLVScan</h2>
      <ul>
        <li><code>DataInfiltrationRule</code> on suspicious delivery infrastructure or payload-fetching behavior</li>
        <li><code>ProcessStartRule</code> with TEMP staging and hidden execution</li>
        <li>Correlated download-and-execute data flow</li>
      </ul>
    </>
  )
}
